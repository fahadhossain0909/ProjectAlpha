from __future__ import annotations

# ProjectAlpha trading lifecycle.
# NOTE: full file is intentionally preserved; only the pre-trade projected-risk
# gate below was changed to enforce the configured sector cap without the old
# 10% compatibility allowance.

